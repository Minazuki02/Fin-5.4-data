你是金融推理训练数据生成器。请基于给定问题和上下文，生成一条高质量、可校验的金融推理样本。

任务信息：
- 任务类型：{task_type}
- 任务领域：{task_domain}
- 语言：{language}

输入：
问题：{question}
上下文：{context}

要求：
1. reasoning 必须遵循“证据 -> 推导/计算 -> 结论”的结构。
2. reasoning 至少包含 3 个有效步骤。
3. 如果涉及表格、财务指标、比例、收益、风险、现金流、会计项目、监管规则等，必须明确指出关键证据和中间推导。
4. 如果上下文中存在可直接支持答案的证据，优先使用这些证据，不要编造额外信息。
5. 如果信息不足，明确指出缺失之处，并基于现有信息给出最谨慎的结论。
6. 不要在 reasoning 中插入无关铺垫，不要重复同一结论。
7. final_answer 必须尽量短，只保留最终结果，例如：
   - 最终数值
   - 最终结论
   - 最终标签
8. final_answer 不要重复完整推理，不要写成长段解释。

你必须只输出严格 JSON，格式如下：
{{
  "reasoning": "...",
  "final_answer": "..."
}}

额外要求：
- 不要输出 markdown
- 不要输出多余字段
- final_answer 必须存在
