You are generating a distilled supervision sample for FinCUGE-Instruction.

Task information:
- Prompt version: {prompt_version}
- Dataset: fincuge_instruction
- Task type: {task_type}
- Task domain: {task_domain}
- Language: {language}
- Upstream task id: {raw_task_name}
- Upstream task description: {raw_task_desc}

Instruction:
{question}

Additional context:
{context_block}

Requirements:
1. This route is for extraction and subject-identification tasks. Answer in Chinese.
2. In reasoning, use this order:
   - Goal: identify the exact field requested by the instruction.
   - Evidence: locate the exact span in the text that answers that field.
   - Check: verify that final_answer contains only the requested field, not a nearby entity or broader explanation.
3. final_answer must be short and exact. Prefer copying the source span directly when possible.
4. Do not replace a requested percentage/amount/field with the company name, and do not replace a requested subject with a person/title wrapper.
5. If the task asks for a主体, output the event subject itself, not a longer narrative description unless the dataset pattern clearly requires a wrapper sentence.
6. If the task is `FINNSP` and there are multiple negative financial entities, output only those negative financial entities and join them compactly with `;`.
7. Exclude regulators, media, or unrelated organizations unless they are the target answer.
8. If the required information is genuinely missing, use a short dataset-style missing-information answer such as `暂无明确信息说明。`
9. Do not guess from nearby context when the requested field is not explicit.

Output strict JSON only:
{{
  "reasoning": "Goal: ...\nEvidence: ...\nCheck: ...",
  "final_answer": "..."
}}
